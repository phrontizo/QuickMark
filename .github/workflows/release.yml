name: Release

on:
  workflow_dispatch:
    inputs:
      version:
        description: "Version number (e.g., 1.1.0)"
        required: true
        type: string

permissions: {}

jobs:
  release:
    runs-on: macos-15
    permissions:
      contents: write

    env:
      VERSION: ${{ github.event.inputs.version }}

    steps:
      - uses: actions/checkout@v5
        with:
          fetch-depth: 0

      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Generate Xcode project
        run: xcodegen generate

      - name: Test
        run: xcodebuild test -scheme QuickMark -destination "platform=macOS" -quiet

      - name: Bump version
        run: ./scripts/bump-version.sh "$VERSION"

      - name: Commit and tag
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add QuickMark/Info.plist Markdown/Info.plist DrawIO/Info.plist Structured/Info.plist
          git commit -m "chore: release v${VERSION}"
          git tag "v${VERSION}"
          git push origin main --tags

      - name: Build Release
        run: |
          xcodebuild build \
            -scheme QuickMark \
            -configuration Release \
            -destination "platform=macOS" \
            -derivedDataPath build/DerivedData \
            CODE_SIGN_IDENTITY="-" \
            -quiet

      - name: Create DMG
        run: |
          APP_PATH=$(find build/DerivedData -name "QuickMark.app" -type d | head -1)
          DMG_NAME="QuickMark-v${VERSION}.dmg"
          STAGING="build/dmg-staging"

          mkdir -p "$STAGING"
          cp -R "$APP_PATH" "$STAGING/"
          ln -s /Applications "$STAGING/Applications"

          hdiutil create -volname "QuickMark" \
            -srcfolder "$STAGING" \
            -ov -format UDZO \
            "$DMG_NAME"

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: "v${{ env.VERSION }}"
          name: "QuickMark v${{ env.VERSION }}"
          files: "QuickMark-v${{ env.VERSION }}.dmg"
          generate_release_notes: true
